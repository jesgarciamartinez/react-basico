/*
    Create two themes ( objects with properties like primaryColor, secondaryColor).
    Provide one of them through Context.
    Consume it from UI components
    Make a button that will change to the other theme when clicked
 */
