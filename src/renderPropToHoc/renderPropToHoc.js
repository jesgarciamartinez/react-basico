/**
 * A function to create a HOC from any render prop component
 * Example:
 * const withCounter = renderPropToHOC(CounterRenderPropsComponent)
 */
function renderPropToHOC(Component) {}
export default renderPropToHOC
