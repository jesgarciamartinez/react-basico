/**
 * A notifications Component that shows a toast with a _close_ button that deletes the notification.
 * The component should consume a notifications context - a list of objects with {text, severity}
 */
